"""AUTOGENERATED by codegen/generate.py — do not edit by hand.

Regenerate with `py codegen/generate.py`; `py codegen/check.py` gates CI.

Source of truth is the Form4API OpenAPI document, so a new backend endpoint
reaches the SDK by regenerating rather than by hand-writing it in two languages.

Field names are snake_case because the client runs every response through
_normalise() before it reaches these classes.
"""

from __future__ import annotations

from dataclasses import dataclass, fields

@dataclass
class AmendmentMetrics:
    superseded_transactions: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "AmendmentMetrics":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class ClusterInsiderEntry:
    insider_cik: str | None = None
    insider_name: str | None = None
    role: str | None = None
    trades: list[ClusterTradeEntry] | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ClusterInsiderEntry":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("trades"), list):
            kwargs["trades"] = [ClusterTradeEntry._from_dict(i) if isinstance(i, dict) else i for i in kwargs["trades"]]
        return cls(**kwargs)


@dataclass
class ClusterTradeEntry:
    code: str | None = None
    price_per_share: float | None = None
    shares: float | None = None
    transaction_date: str | None = None
    value: float | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ClusterTradeEntry":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CompanyResponse:
    active_insiders: int | None = None
    cik: str | None = None
    exchange: str | None = None
    name: str | None = None
    sic_description: str | None = None
    state_of_incorporation: str | None = None
    ticker: str | None = None
    total_filings: int | None = None
    website: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CompanyResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CongressPoliticianProfileResponse:
    bioguide_id: str | None = None
    buys: int | None = None
    chamber: str | None = None
    full_name: str | None = None
    last_trade_disclosed_at: str | None = None
    party: str | None = None
    recent_trades: list[CongressTradeDto] | None = None
    sells: int | None = None
    slug: str | None = None
    state: str | None = None
    top_tickers: list[CongressTickerCountDto] | None = None
    total_trades: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CongressPoliticianProfileResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("recent_trades"), list):
            kwargs["recent_trades"] = [CongressTradeDto._from_dict(i) if isinstance(i, dict) else i for i in kwargs["recent_trades"]]
        if isinstance(kwargs.get("top_tickers"), list):
            kwargs["top_tickers"] = [CongressTickerCountDto._from_dict(i) if isinstance(i, dict) else i for i in kwargs["top_tickers"]]
        return cls(**kwargs)


@dataclass
class CongressPoliticianRefDto:
    bioguide_id: str | None = None
    chamber: str | None = None
    full_name: str | None = None
    party: str | None = None
    slug: str | None = None
    state: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CongressPoliticianRefDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CongressPoliticianRollupDto:
    bioguide_id: str | None = None
    buys: int | None = None
    chamber: str | None = None
    full_name: str | None = None
    last_trade_disclosed_at: str | None = None
    party: str | None = None
    sells: int | None = None
    slug: str | None = None
    state: str | None = None
    total_trades: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CongressPoliticianRollupDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CongressTickerCountDto:
    ticker: str | None = None
    trade_count: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CongressTickerCountDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CongressTickerPoliticianEntryDto:
    bioguide_id: str | None = None
    buys: int | None = None
    chamber: str | None = None
    full_name: str | None = None
    party: str | None = None
    sells: int | None = None
    slug: str | None = None
    state: str | None = None
    trade_count: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CongressTickerPoliticianEntryDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CongressTickerRollupResponse:
    buys: int | None = None
    politicians: list[CongressTickerPoliticianEntryDto] | None = None
    sells: int | None = None
    ticker: str | None = None
    total_trades: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CongressTickerRollupResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("politicians"), list):
            kwargs["politicians"] = [CongressTickerPoliticianEntryDto._from_dict(i) if isinstance(i, dict) else i for i in kwargs["politicians"]]
        return cls(**kwargs)


@dataclass
class CongressTradeDto:
    amount_high: float | None = None
    amount_low: float | None = None
    asset_name: str | None = None
    asset_type: str | None = None
    disclosure_date: str | None = None
    disclosure_lag_days: int | None = None
    owner_type: str | None = None
    politician: CongressPoliticianRefDto | None = None
    ticker: str | None = None
    transaction_date: str | None = None
    transaction_type: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CongressTradeDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("politician"), dict):
            kwargs["politician"] = CongressPoliticianRefDto._from_dict(kwargs["politician"])
        return cls(**kwargs)


@dataclass
class ConvergenceCongressLegDto:
    amount_high: float | None = None
    amount_low: float | None = None
    bioguide_id: str | None = None
    disclosure_date: str | None = None
    disclosure_lag_days: int | None = None
    full_name: str | None = None
    party: str | None = None
    transaction_date: str | None = None
    transaction_type: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ConvergenceCongressLegDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class ConvergenceEntryDto:
    company_name: str | None = None
    congress: list[ConvergenceCongressLegDto] | None = None
    first_seen: str | None = None
    insider: ConvergenceInsiderSideDto | None = None
    last_seen: str | None = None
    strength: int | None = None
    ticker: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ConvergenceEntryDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("congress"), list):
            kwargs["congress"] = [ConvergenceCongressLegDto._from_dict(i) if isinstance(i, dict) else i for i in kwargs["congress"]]
        if isinstance(kwargs.get("insider"), dict):
            kwargs["insider"] = ConvergenceInsiderSideDto._from_dict(kwargs["insider"])
        return cls(**kwargs)


@dataclass
class ConvergenceInsiderSideDto:
    insider_count: int | None = None
    signal_date: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ConvergenceInsiderSideDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CorpusStats:
    companies: int | None = None
    filings: int | None = None
    form13_f_filings: int | None = None
    form144_filings: int | None = None
    history_start: str | None = None
    holdings: int | None = None
    ingestion_latency: IngestionLatencyStats | None = None
    latest_quarter_aum: float | None = None
    transactions: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CorpusStats":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("ingestion_latency"), dict):
            kwargs["ingestion_latency"] = IngestionLatencyStats._from_dict(kwargs["ingestion_latency"])
        return cls(**kwargs)


@dataclass
class CoverageMetrics:
    companies_tracked: int | None = None
    cusip_resolved_pct: float | None = None
    form13_f_filings: int | None = None
    form144_filings: int | None = None
    form4_filings: int | None = None
    tickers_with_price_data: int | None = None
    transactions_total: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CoverageMetrics":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CreateKeyRequest:
    label: str | None = None
    plan: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CreateKeyRequest":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class CreateWebhookRequest:
    event_types: list[str] | None = None
    url: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "CreateWebhookRequest":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class DataQualityResponse:
    amendments: AmendmentMetrics | None = None
    as_of: str | None = None
    coverage: CoverageMetrics | None = None
    freshness: FreshnessMetrics | None = None
    returns: ReturnsCoverage | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "DataQualityResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("amendments"), dict):
            kwargs["amendments"] = AmendmentMetrics._from_dict(kwargs["amendments"])
        if isinstance(kwargs.get("coverage"), dict):
            kwargs["coverage"] = CoverageMetrics._from_dict(kwargs["coverage"])
        if isinstance(kwargs.get("freshness"), dict):
            kwargs["freshness"] = FreshnessMetrics._from_dict(kwargs["freshness"])
        if isinstance(kwargs.get("returns"), dict):
            kwargs["returns"] = ReturnsCoverage._from_dict(kwargs["returns"])
        return cls(**kwargs)


@dataclass
class DirectoryEntryResponse:
    cik: str | None = None
    filer_group_size: int | None = None
    is_director: bool | None = None
    is_officer: bool | None = None
    is_ten_percent_owner: bool | None = None
    last_filed_at: str | None = None
    name: str | None = None
    officer_title: str | None = None
    primary_company_name: str | None = None
    primary_ticker: str | None = None
    transaction_count: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "DirectoryEntryResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class DirectoryLetter:
    count: int | None = None
    letter: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "DirectoryLetter":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class ExcludedTradeEntry:
    code: str | None = None
    insider_cik: str | None = None
    insider_name: str | None = None
    reason: str | None = None
    shares: float | None = None
    transaction_date: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ExcludedTradeEntry":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class FilingResponse:
    accession_number: str | None = None
    amendment_type: str | None = None
    company_name: str | None = None
    company_ticker: str | None = None
    filed_at: str | None = None
    period_of_report: str | None = None
    transaction_count: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "FilingResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class Form144Response:
    accession_number: str | None = None
    acquired_date: str | None = None
    aggregate_market_value: float | None = None
    approx_sale_date: str | None = None
    broker: str | None = None
    company_name: str | None = None
    exchange: str | None = None
    filed_at: str | None = None
    insider_name: str | None = None
    is_under10b5_plan: bool | None = None
    nature_of_acquisition: str | None = None
    notice_date: str | None = None
    relationship: str | None = None
    shares_proposed: float | None = None
    ticker: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "Form144Response":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class Form4HealthCheck:
    age_minutes: int | None = None
    latest_processed_at: str | None = None
    status: str | None = None
    threshold_minutes: int | None = None
    within_activity_window: bool | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "Form4HealthCheck":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class FreshnessMetrics:
    ingestion_latency_sample_size: int | None = None
    latest_form4_processed_at: str | None = None
    latest_price_bar_date: str | None = None
    median_ingestion_latency_seconds: int | None = None
    p95_ingestion_latency_seconds: int | None = None
    price_data_days_behind: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "FreshnessMetrics":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class HoldingResponse:
    accession_number: str | None = None
    cusip: str | None = None
    filed_at: str | None = None
    investment_discretion: str | None = None
    issuer_name: str | None = None
    manager: str | None = None
    manager_cik: str | None = None
    report_period: str | None = None
    share_type: str | None = None
    shares: float | None = None
    ticker: str | None = None
    title_of_class: str | None = None
    value: float | None = None
    voting_none: float | None = None
    voting_shared: float | None = None
    voting_sole: float | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "HoldingResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class IngestionHealthResponse:
    as_of: str | None = None
    form4: Form4HealthCheck | None = None
    prices: PricesHealthCheck | None = None
    queue: QueueHealthCheck | None = None
    status: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "IngestionHealthResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("form4"), dict):
            kwargs["form4"] = Form4HealthCheck._from_dict(kwargs["form4"])
        if isinstance(kwargs.get("prices"), dict):
            kwargs["prices"] = PricesHealthCheck._from_dict(kwargs["prices"])
        if isinstance(kwargs.get("queue"), dict):
            kwargs["queue"] = QueueHealthCheck._from_dict(kwargs["queue"])
        return cls(**kwargs)


@dataclass
class IngestionLatencyStats:
    p50_seconds: float | None = None
    p95_seconds: float | None = None
    sample_count: int | None = None
    window: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "IngestionLatencyStats":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class Insider10b5Split:
    pct_under10b5_plan: float | None = None
    total_discretionary: int | None = None
    total_under_plan: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "Insider10b5Split":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class InsiderCareer:
    companies: list[InsiderCompanyEntry] | None = None
    first_transaction: str | None = None
    last_transaction: str | None = None
    net_usd: float | None = None
    returns: InsiderReturnsSummary | None = None
    ten_b5_plan: Insider10b5Split | None = None
    total_bought_usd: float | None = None
    total_sold_usd: float | None = None
    total_transactions: int | None = None
    transaction_type_breakdown: InsiderTxCodeBreakdown | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderCareer":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("companies"), list):
            kwargs["companies"] = [InsiderCompanyEntry._from_dict(i) if isinstance(i, dict) else i for i in kwargs["companies"]]
        if isinstance(kwargs.get("returns"), dict):
            kwargs["returns"] = InsiderReturnsSummary._from_dict(kwargs["returns"])
        if isinstance(kwargs.get("ten_b5_plan"), dict):
            kwargs["ten_b5_plan"] = Insider10b5Split._from_dict(kwargs["ten_b5_plan"])
        if isinstance(kwargs.get("transaction_type_breakdown"), dict):
            kwargs["transaction_type_breakdown"] = InsiderTxCodeBreakdown._from_dict(kwargs["transaction_type_breakdown"])
        return cls(**kwargs)


@dataclass
class InsiderCompanyEntry:
    company_name: str | None = None
    first_seen: str | None = None
    ticker: str | None = None
    transaction_count: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderCompanyEntry":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class InsiderDirectoryResponse:
    entries: list[DirectoryEntryResponse] | None = None
    letter: str | None = None
    letter_total: int | None = None
    letters: list[DirectoryLetter] | None = None
    page: int | None = None
    per_page: int | None = None
    refreshed_at: str | None = None
    total: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderDirectoryResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("entries"), list):
            kwargs["entries"] = [DirectoryEntryResponse._from_dict(i) if isinstance(i, dict) else i for i in kwargs["entries"]]
        if isinstance(kwargs.get("letters"), list):
            kwargs["letters"] = [DirectoryLetter._from_dict(i) if isinstance(i, dict) else i for i in kwargs["letters"]]
        return cls(**kwargs)


@dataclass
class InsiderLeaderboardResponse:
    insiders: list[LeaderboardEntry] | None = None
    methodology: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderLeaderboardResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("insiders"), list):
            kwargs["insiders"] = [LeaderboardEntry._from_dict(i) if isinstance(i, dict) else i for i in kwargs["insiders"]]
        return cls(**kwargs)


@dataclass
class InsiderResponse:
    cik: str | None = None
    is_director: bool | None = None
    is_officer: bool | None = None
    is_ten_percent_owner: bool | None = None
    name: str | None = None
    officer_title: str | None = None
    total_filings: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class InsiderReturnsSummary:
    avg_return1d: float | None = None
    avg_return1m: float | None = None
    avg_return1w: float | None = None
    avg_return3m: float | None = None
    avg_return6m: float | None = None
    open_market_transactions_with_returns: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderReturnsSummary":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class InsiderScorecardResponse:
    avg_return3m: float | None = None
    avg_return6m: float | None = None
    best_buy: ScorecardTradeRef | None = None
    hit_rate3m: float | None = None
    hit_rate6m: float | None = None
    insider_cik: str | None = None
    insider_name: str | None = None
    last_trade_at: str | None = None
    median_return3m: float | None = None
    median_return6m: float | None = None
    methodology: str | None = None
    sample_sufficient: bool | None = None
    sample_sufficient6m: bool | None = None
    scored_buy_count: int | None = None
    scored_buy_count6m: int | None = None
    worst_buy: ScorecardTradeRef | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderScorecardResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("best_buy"), dict):
            kwargs["best_buy"] = ScorecardTradeRef._from_dict(kwargs["best_buy"])
        if isinstance(kwargs.get("worst_buy"), dict):
            kwargs["worst_buy"] = ScorecardTradeRef._from_dict(kwargs["worst_buy"])
        return cls(**kwargs)


@dataclass
class InsiderSummaryResponse:
    career: InsiderCareer | None = None
    cik: str | None = None
    is_director: bool | None = None
    is_officer: bool | None = None
    is_ten_percent_owner: bool | None = None
    name: str | None = None
    officer_title: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderSummaryResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("career"), dict):
            kwargs["career"] = InsiderCareer._from_dict(kwargs["career"])
        return cls(**kwargs)


@dataclass
class InsiderTxCodeBreakdown:
    a: int | None = None
    f: int | None = None
    g: int | None = None
    m: int | None = None
    other: int | None = None
    p: int | None = None
    s: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InsiderTxCodeBreakdown":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class InstitutionalOwnershipDto:
    coverage_incomplete: bool | None = None
    delta_qoq_pct: float | None = None
    quarter: str | None = None
    top_holders: list[TopHolderDto] | None = None
    total_aum_usd: float | None = None
    trend: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "InstitutionalOwnershipDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("top_holders"), list):
            kwargs["top_holders"] = [TopHolderDto._from_dict(i) if isinstance(i, dict) else i for i in kwargs["top_holders"]]
        return cls(**kwargs)


@dataclass
class LeaderboardEntry:
    avg_return: float | None = None
    hit_rate: float | None = None
    insider_cik: str | None = None
    insider_name: str | None = None
    last_trade_at: str | None = None
    scored_buy_count: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "LeaderboardEntry":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class ManagerResponse:
    cik: str | None = None
    latest_accession_number: str | None = None
    latest_aum: float | None = None
    latest_filed_at: str | None = None
    latest_position_count: int | None = None
    latest_report_period: str | None = None
    name: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ManagerResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class PricesHealthCheck:
    days_behind: int | None = None
    latest_bar_date: str | None = None
    status: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "PricesHealthCheck":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class QueueHealthCheck:
    oldest_pending_age_minutes: int | None = None
    pending_count: int | None = None
    status: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "QueueHealthCheck":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class RatioBasis:
    buy_sell_ratio: float | None = None
    buy_shares: float | None = None
    sell_shares: float | None = None
    window_days: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "RatioBasis":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class ReturnsCoverage:
    coverage_pct: float | None = None
    eligible_transactions: int | None = None
    fully_computed: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ReturnsCoverage":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class ScorecardTradeRef:
    filed_at: str | None = None
    return3m: float | None = None
    ticker: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "ScorecardTradeRef":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class SentimentMonthEntry:
    buy_count: int | None = None
    buy_value: float | None = None
    period: str | None = None
    score: float | None = None
    sell_count: int | None = None
    sell_value: float | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "SentimentMonthEntry":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class SentimentResponse:
    company_name: str | None = None
    monthly: list[SentimentMonthEntry] | None = None
    ticker: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "SentimentResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("monthly"), list):
            kwargs["monthly"] = [SentimentMonthEntry._from_dict(i) if isinstance(i, dict) else i for i in kwargs["monthly"]]
        return cls(**kwargs)


@dataclass
class SignalCriteria:
    cluster_counts_exclude10b5: bool | None = None
    cluster_threshold: int | None = None
    cluster_window_days: int | None = None
    ratio_window_days: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "SignalCriteria":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class SignalExplanation:
    buy_sell_ratio: float | None = None
    cluster_buyers: list[ClusterInsiderEntry] | None = None
    cluster_sellers: list[object] | None = None
    company_name: str | None = None
    criteria: SignalCriteria | None = None
    excluded: list[ExcludedTradeEntry] | None = None
    insider_count: int | None = None
    is_cluster_buy: bool | None = None
    is_cluster_sell: bool | None = None
    note: str | None = None
    ratio: RatioBasis | None = None
    signal_date: str | None = None
    ticker: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "SignalExplanation":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("cluster_buyers"), list):
            kwargs["cluster_buyers"] = [ClusterInsiderEntry._from_dict(i) if isinstance(i, dict) else i for i in kwargs["cluster_buyers"]]
        if isinstance(kwargs.get("criteria"), dict):
            kwargs["criteria"] = SignalCriteria._from_dict(kwargs["criteria"])
        if isinstance(kwargs.get("excluded"), list):
            kwargs["excluded"] = [ExcludedTradeEntry._from_dict(i) if isinstance(i, dict) else i for i in kwargs["excluded"]]
        if isinstance(kwargs.get("ratio"), dict):
            kwargs["ratio"] = RatioBasis._from_dict(kwargs["ratio"])
        return cls(**kwargs)


@dataclass
class SignalResponse:
    buy_sell_ratio: float | None = None
    company_name: str | None = None
    insider_count: int | None = None
    is_cluster_buy: bool | None = None
    is_cluster_sell: bool | None = None
    signal_date: str | None = None
    ticker: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "SignalResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class TestimonialSubmitRequest:
    author_company: str | None = None
    author_email: str | None = None
    author_name: str | None = None
    author_title: str | None = None
    consent_public: bool | None = None
    link_url: str | None = None
    quote: str | None = None
    rating: int | None = None
    referral_source: str | None = None
    video_url: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "TestimonialSubmitRequest":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class TopHolderDto:
    manager_cik: str | None = None
    manager_name: str | None = None
    shares: float | None = None
    value: float | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "TopHolderDto":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class TransactionResponse:
    accession_number: str | None = None
    company_name: str | None = None
    direct_indirect: str | None = None
    insider_cik: str | None = None
    insider_name: str | None = None
    insider_title: str | None = None
    institutional_ownership: InstitutionalOwnershipDto | None = None
    is10_pct_owner: bool | None = None
    is10b5_plan: bool | None = None
    is_derivative: bool | None = None
    is_director: bool | None = None
    is_officer: bool | None = None
    is_open_market: bool | None = None
    period_of_report: str | None = None
    price_per_share: float | None = None
    return1d: float | None = None
    return1m: float | None = None
    return1w: float | None = None
    return3m: float | None = None
    return6m: float | None = None
    security_title: str | None = None
    shares_amount: float | None = None
    shares_owned_after: float | None = None
    ticker: str | None = None
    total_value: float | None = None
    transaction_code: str | None = None
    transaction_date: str | None = None
    value_quality: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "TransactionResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("institutional_ownership"), dict):
            kwargs["institutional_ownership"] = InstitutionalOwnershipDto._from_dict(kwargs["institutional_ownership"])
        return cls(**kwargs)


@dataclass
class UptimeDayBucket:
    date: str | None = None
    expected: int | None = None
    healthy: int | None = None
    uptime_pct: float | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "UptimeDayBucket":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


@dataclass
class UptimeHistoryResponse:
    days: list[UptimeDayBucket] | None = None
    overall_pct: float | None = None
    start: str | None = None
    window_days: int | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "UptimeHistoryResponse":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        kwargs = {k: v for k, v in data.items() if k in known}
        if isinstance(kwargs.get("days"), list):
            kwargs["days"] = [UptimeDayBucket._from_dict(i) if isinstance(i, dict) else i for i in kwargs["days"]]
        return cls(**kwargs)


@dataclass
class WaitlistRequest:
    interval: str | None = None
    plan: str | None = None
    source: str | None = None

    @classmethod
    def _from_dict(cls, data: dict) -> "WaitlistRequest":
        """Build from an API payload, ignoring unknown keys.

        Constructing with **data directly (as the hand-written resources do)
        means the SDK raises TypeError the moment the backend adds a field.
        Filtering keeps older SDK versions working against a newer API."""
        known = {f.name for f in fields(cls)}
        return cls(**{k: v for k, v in data.items() if k in known})


class GeneratedCompaniesResource:
    def __init__(self, client) -> None:
        self._client = client

    def list(self, *, sort: str | None = None, limit: int | None = None) -> list[CompanyResponse]:
        """List companies with a public ticker, sorted by name or total filings

        Returns a single page of companies that have a tracked public ticker — for browsing or building a company picker, not for searching by name or CIK (there is no full-text search here; use GET /v1/companies/{ticker} to fetch one company by its exact ticker). Each entry includes the company's CIK, name, ticker, exchange, total filing count, and distinct insider count. There is no page parameter — this endpoint always returns the top `limit` companies by the chosen sort order. Not plan-gated."""
        params = {
            "sort": sort,
            "limit": limit,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/companies", params=params)
        return [CompanyResponse._from_dict(item) for item in data]


class GeneratedAsyncCompaniesResource:
    def __init__(self, client) -> None:
        self._client = client

    async def list(self, *, sort: str | None = None, limit: int | None = None) -> list[CompanyResponse]:
        """List companies with a public ticker, sorted by name or total filings

        Returns a single page of companies that have a tracked public ticker — for browsing or building a company picker, not for searching by name or CIK (there is no full-text search here; use GET /v1/companies/{ticker} to fetch one company by its exact ticker). Each entry includes the company's CIK, name, ticker, exchange, total filing count, and distinct insider count. There is no page parameter — this endpoint always returns the top `limit` companies by the chosen sort order. Not plan-gated."""
        params = {
            "sort": sort,
            "limit": limit,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/companies", params=params)
        return [CompanyResponse._from_dict(item) for item in data]


class GeneratedCongressResource:
    def __init__(self, client) -> None:
        self._client = client

    def politician(self, id_or_slug: str, *, top_tickers: int | None = None, recent_trades: int | None = None) -> CongressPoliticianProfileResponse:
        """Get one politician's congressional trading profile (Pro plan+)

        Returns one politician's profile — identity fields, total/buy/sell trade counts, most recent trade's disclosure date, their top N most-traded tickers (by trade count), and their N most recent trades (same shape as GET /v1/congress/trades). Use this for a one-call politician overview rather than paging /v1/congress/trades?politician= yourself. Accepts either a bioguide ID (e.g. "P000197") or the politician's URL slug (e.g. "nancy-pelosi") in the path, matched case-insensitively against whichever field applies. Returns 404 NOT_FOUND if neither matches. Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Query runs live — no caching."""
        params = {
            "top_tickers": top_tickers,
            "recent_trades": recent_trades,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/congress/politicians/{id_or_slug}", params=params)
        return CongressPoliticianProfileResponse._from_dict(data)

    def politicians(self, *, page: int | None = None, per_page: int | None = None) -> list[CongressPoliticianRollupDto]:
        """Ranked rollup of politicians by congressional trade activity (Pro plan+)

        Returns a paginated list of politicians who have at least one non-superseded congressional trade, each with total/buy/sell counts (sells include both Sale and PartialSale; Exchange trades count only toward total) and their most recent trade's disclosure date. Ordered by total trade count descending, ties broken by most recently disclosed. Use this to discover active traders; for one politician's full profile (including their most-traded tickers and recent trades) use GET /v1/congress/politicians/{idOrSlug}. Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Query runs live — no caching."""
        params = {
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/congress/politicians", params=params)
        return [CongressPoliticianRollupDto._from_dict(item) for item in data]

    def ticker(self, ticker: str, *, window_days: int | None = None) -> CongressTickerRollupResponse:
        """Which politicians traded a ticker, with net buy/sell counts (Pro plan+)

        Returns every politician who has a non-superseded congressional trade in the given ticker, each with their trade/buy/sell counts, plus ticker-level totals. Optional window_days restricts to trades with a transactionDate in the trailing N days; omit for all-time. A ticker with no congress trades returns 200 with an empty politicians array and zero counts rather than 404 — there is no separate ticker/company entity in this dataset to 404 against. Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Query runs live — no caching."""
        params = {
            "window_days": window_days,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/congress/tickers/{ticker}", params=params)
        return CongressTickerRollupResponse._from_dict(data)

    def trades(self, *, ticker: str | None = None, politician: str | None = None, party: str | None = None, chamber: str | None = None, state: str | None = None, transaction_type: str | None = None, min_amount: float | None = None, transaction_date_from: str | None = None, transaction_date_to: str | None = None, disclosure_date_from: str | None = None, disclosure_date_to: str | None = None, page: int | None = None, per_page: int | None = None) -> list[CongressTradeDto]:
        """Query congressional STOCK Act trades (Free+, plan-clamped disclosure window)

        Returns a paginated JSON list of congressional periodic-transaction-report trades, most recently DISCLOSED first, with non-superseded rows only (amended-away rows never appear). COVERAGE — HOUSE ONLY TODAY: every trade in this dataset comes from the U.S. House Clerk's PTR index. Senate eFD (efdsearch.senate.gov) returns 403 to datacenter traffic, so no Senate filings are ingested yet. chamber=Senate remains a valid filter but matches nothing and returns the response header X-Coverage-Note: chamber-not-covered, so an empty result is never ambiguous. Scanning by chamber should treat that header as "not covered", not as "no trades". PLAN-CLAMPED WINDOW: this endpoint is open to every plan, but how far back you can see is clamped on disclosureDate — Free sees only trades disclosed in the last 30 days, Starter the last 366 days, Pro/Business/Enterprise unlimited history. Passing an older disclosure_date_from than your plan allows does not extend the window — the floor always wins. Filters: ticker, politician (bioguideId, exact), party (free-text, case-insensitive exact match — not a fixed enum), chamber (House|Senate — see the coverage note above), state (2-letter code), transaction_type (purchase|sale|partial_sale|exchange), min_amount (range-aware — matches AmountLow >= value, never a fabricated midpoint), transaction_date_from/to, disclosure_date_from/to. Every row always carries BOTH amountLow and amountHigh (STOCK Act discloses ranges, never exact figures) and disclosureLagDays = (disclosureDate - transactionDate) — the STOCK Act allows up to 45 days of lag, so "real-time" here means minutes-after-disclosure, not minutes-after-trade. For per-politician or per-ticker rollups use GET /v1/congress/politicians, /v1/congress/politicians/{idOrSlug}, or /v1/congress/tickers/{ticker} (all Pro+). Query runs live against the database — no caching."""
        params = {
            "ticker": ticker,
            "politician": politician,
            "party": party,
            "chamber": chamber,
            "state": state,
            "transaction_type": transaction_type,
            "min_amount": min_amount,
            "transaction_date_from": transaction_date_from,
            "transaction_date_to": transaction_date_to,
            "disclosure_date_from": disclosure_date_from,
            "disclosure_date_to": disclosure_date_to,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/congress/trades", params=params)
        return [CongressTradeDto._from_dict(item) for item in data]


class GeneratedAsyncCongressResource:
    def __init__(self, client) -> None:
        self._client = client

    async def politician(self, id_or_slug: str, *, top_tickers: int | None = None, recent_trades: int | None = None) -> CongressPoliticianProfileResponse:
        """Get one politician's congressional trading profile (Pro plan+)

        Returns one politician's profile — identity fields, total/buy/sell trade counts, most recent trade's disclosure date, their top N most-traded tickers (by trade count), and their N most recent trades (same shape as GET /v1/congress/trades). Use this for a one-call politician overview rather than paging /v1/congress/trades?politician= yourself. Accepts either a bioguide ID (e.g. "P000197") or the politician's URL slug (e.g. "nancy-pelosi") in the path, matched case-insensitively against whichever field applies. Returns 404 NOT_FOUND if neither matches. Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Query runs live — no caching."""
        params = {
            "top_tickers": top_tickers,
            "recent_trades": recent_trades,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/congress/politicians/{id_or_slug}", params=params)
        return CongressPoliticianProfileResponse._from_dict(data)

    async def politicians(self, *, page: int | None = None, per_page: int | None = None) -> list[CongressPoliticianRollupDto]:
        """Ranked rollup of politicians by congressional trade activity (Pro plan+)

        Returns a paginated list of politicians who have at least one non-superseded congressional trade, each with total/buy/sell counts (sells include both Sale and PartialSale; Exchange trades count only toward total) and their most recent trade's disclosure date. Ordered by total trade count descending, ties broken by most recently disclosed. Use this to discover active traders; for one politician's full profile (including their most-traded tickers and recent trades) use GET /v1/congress/politicians/{idOrSlug}. Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Query runs live — no caching."""
        params = {
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/congress/politicians", params=params)
        return [CongressPoliticianRollupDto._from_dict(item) for item in data]

    async def ticker(self, ticker: str, *, window_days: int | None = None) -> CongressTickerRollupResponse:
        """Which politicians traded a ticker, with net buy/sell counts (Pro plan+)

        Returns every politician who has a non-superseded congressional trade in the given ticker, each with their trade/buy/sell counts, plus ticker-level totals. Optional window_days restricts to trades with a transactionDate in the trailing N days; omit for all-time. A ticker with no congress trades returns 200 with an empty politicians array and zero counts rather than 404 — there is no separate ticker/company entity in this dataset to 404 against. Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Query runs live — no caching."""
        params = {
            "window_days": window_days,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/congress/tickers/{ticker}", params=params)
        return CongressTickerRollupResponse._from_dict(data)

    async def trades(self, *, ticker: str | None = None, politician: str | None = None, party: str | None = None, chamber: str | None = None, state: str | None = None, transaction_type: str | None = None, min_amount: float | None = None, transaction_date_from: str | None = None, transaction_date_to: str | None = None, disclosure_date_from: str | None = None, disclosure_date_to: str | None = None, page: int | None = None, per_page: int | None = None) -> list[CongressTradeDto]:
        """Query congressional STOCK Act trades (Free+, plan-clamped disclosure window)

        Returns a paginated JSON list of congressional periodic-transaction-report trades, most recently DISCLOSED first, with non-superseded rows only (amended-away rows never appear). COVERAGE — HOUSE ONLY TODAY: every trade in this dataset comes from the U.S. House Clerk's PTR index. Senate eFD (efdsearch.senate.gov) returns 403 to datacenter traffic, so no Senate filings are ingested yet. chamber=Senate remains a valid filter but matches nothing and returns the response header X-Coverage-Note: chamber-not-covered, so an empty result is never ambiguous. Scanning by chamber should treat that header as "not covered", not as "no trades". PLAN-CLAMPED WINDOW: this endpoint is open to every plan, but how far back you can see is clamped on disclosureDate — Free sees only trades disclosed in the last 30 days, Starter the last 366 days, Pro/Business/Enterprise unlimited history. Passing an older disclosure_date_from than your plan allows does not extend the window — the floor always wins. Filters: ticker, politician (bioguideId, exact), party (free-text, case-insensitive exact match — not a fixed enum), chamber (House|Senate — see the coverage note above), state (2-letter code), transaction_type (purchase|sale|partial_sale|exchange), min_amount (range-aware — matches AmountLow >= value, never a fabricated midpoint), transaction_date_from/to, disclosure_date_from/to. Every row always carries BOTH amountLow and amountHigh (STOCK Act discloses ranges, never exact figures) and disclosureLagDays = (disclosureDate - transactionDate) — the STOCK Act allows up to 45 days of lag, so "real-time" here means minutes-after-disclosure, not minutes-after-trade. For per-politician or per-ticker rollups use GET /v1/congress/politicians, /v1/congress/politicians/{idOrSlug}, or /v1/congress/tickers/{ticker} (all Pro+). Query runs live against the database — no caching."""
        params = {
            "ticker": ticker,
            "politician": politician,
            "party": party,
            "chamber": chamber,
            "state": state,
            "transaction_type": transaction_type,
            "min_amount": min_amount,
            "transaction_date_from": transaction_date_from,
            "transaction_date_to": transaction_date_to,
            "disclosure_date_from": disclosure_date_from,
            "disclosure_date_to": disclosure_date_to,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/congress/trades", params=params)
        return [CongressTradeDto._from_dict(item) for item in data]


class GeneratedDataQualityResource:
    def __init__(self, client) -> None:
        self._client = client

    def get(self) -> DataQualityResponse:
        """Public data-quality, freshness and coverage metrics for the whole dataset

        Returns public, keyless metrics on data freshness, ingestion latency, corpus coverage, and post-trade returns coverage — use this to check whether the dataset is current before relying on it (e.g. confirm Form 4 ingestion isn't stalled, or that price data isn't stale), not to look up any single company, insider, or transaction. Includes: most recent Form 4 processed timestamp and median/p95 filing-accepted-to-processed latency in seconds, latest price-bar date and how many days behind it is, total companies/transactions tracked plus filing counts by form type (4, 144, 13F-HR), the percentage of 13F CUSIPs resolved to a ticker, and the percentage of eligible transactions with fully computed post-trade returns. Takes no parameters. Cached for 30 minutes; no API key or plan required."""
        data = self._client._get(f"/v1/data-quality")
        return DataQualityResponse._from_dict(data)


class GeneratedAsyncDataQualityResource:
    def __init__(self, client) -> None:
        self._client = client

    async def get(self) -> DataQualityResponse:
        """Public data-quality, freshness and coverage metrics for the whole dataset

        Returns public, keyless metrics on data freshness, ingestion latency, corpus coverage, and post-trade returns coverage — use this to check whether the dataset is current before relying on it (e.g. confirm Form 4 ingestion isn't stalled, or that price data isn't stale), not to look up any single company, insider, or transaction. Includes: most recent Form 4 processed timestamp and median/p95 filing-accepted-to-processed latency in seconds, latest price-bar date and how many days behind it is, total companies/transactions tracked plus filing counts by form type (4, 144, 13F-HR), the percentage of 13F CUSIPs resolved to a ticker, and the percentage of eligible transactions with fully computed post-trade returns. Takes no parameters. Cached for 30 minutes; no API key or plan required."""
        data = await self._client._get(f"/v1/data-quality")
        return DataQualityResponse._from_dict(data)


class GeneratedFilingsResource:
    def __init__(self, client) -> None:
        self._client = client

    def get(self, accession: str) -> FilingResponse:
        """Get a single Form 4 filing by its exact SEC accession number

        Returns one filing's metadata — accession number, company ticker/name, period of report, filed date, amendment type (Original/Amendment), and the count of non-superseded transactions it contains. Use this to look up a specific filing you already have the accession number for (e.g. from GET /v1/filings/recent or GET /v1/transactions); it does not return the individual transaction rows themselves — pull those via GET /v1/transactions filtered by ticker/cik and date. Returns 404 NOT_FOUND if the accession number isn't tracked. Not plan-gated. Query runs live against the database — no caching."""
        data = self._client._get(f"/v1/filings/{accession}")
        return FilingResponse._from_dict(data)

    def list(self, *, ticker: str | None = None, cik: str | None = None, from_: str | None = None, to: str | None = None, page: int | None = None, per_page: int | None = None, limit: int | None = None) -> list[FilingResponse]:
        """List Form 4 filings with optional ticker, CIK and date filters

        Returns a paginated list of Form 4 filings, newest filed first. Filter by ticker, cik, and a from/to filed-date window. Each entry carries the accession number, company ticker/name, period of report, filed date, amendment type (Original/Amendment), and the count of non-superseded transactions in that filing. Use this for a company's filing HISTORY; use GET /v1/filings/recent for a live newest-first feed (it has no page parameter), and GET /v1/transactions when you want the individual trades rather than the filings that contain them. `limit` is accepted as an alias for `per_page`. Not plan-gated."""
        params = {
            "ticker": ticker,
            "cik": cik,
            "from": from_,
            "to": to,
            "page": page,
            "per_page": per_page,
            "limit": limit,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/filings", params=params)
        return [FilingResponse._from_dict(item) for item in data]

    def recent(self, *, ticker: str | None = None, per_page: int | None = None) -> list[FilingResponse]:
        """Get the most recently filed Form 4s, optionally filtered by ticker

        Returns the most recently filed Form 4s across all companies, newest first, optionally restricted to a single ticker. Use this to monitor new insider activity as it's ingested (e.g. a live "latest filings" feed) rather than for historical or bulk queries — for date-range or filter-heavy queries use GET /v1/transactions with from/to instead. Each entry includes the accession number, company ticker/name, period of report, filed date, amendment type, and the count of non-superseded transactions in that filing. There is no page parameter — this always returns the newest per_page filings, not an arbitrary offset. Not plan-gated."""
        params = {
            "ticker": ticker,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/filings/recent", params=params)
        return [FilingResponse._from_dict(item) for item in data]


class GeneratedAsyncFilingsResource:
    def __init__(self, client) -> None:
        self._client = client

    async def get(self, accession: str) -> FilingResponse:
        """Get a single Form 4 filing by its exact SEC accession number

        Returns one filing's metadata — accession number, company ticker/name, period of report, filed date, amendment type (Original/Amendment), and the count of non-superseded transactions it contains. Use this to look up a specific filing you already have the accession number for (e.g. from GET /v1/filings/recent or GET /v1/transactions); it does not return the individual transaction rows themselves — pull those via GET /v1/transactions filtered by ticker/cik and date. Returns 404 NOT_FOUND if the accession number isn't tracked. Not plan-gated. Query runs live against the database — no caching."""
        data = await self._client._get(f"/v1/filings/{accession}")
        return FilingResponse._from_dict(data)

    async def list(self, *, ticker: str | None = None, cik: str | None = None, from_: str | None = None, to: str | None = None, page: int | None = None, per_page: int | None = None, limit: int | None = None) -> list[FilingResponse]:
        """List Form 4 filings with optional ticker, CIK and date filters

        Returns a paginated list of Form 4 filings, newest filed first. Filter by ticker, cik, and a from/to filed-date window. Each entry carries the accession number, company ticker/name, period of report, filed date, amendment type (Original/Amendment), and the count of non-superseded transactions in that filing. Use this for a company's filing HISTORY; use GET /v1/filings/recent for a live newest-first feed (it has no page parameter), and GET /v1/transactions when you want the individual trades rather than the filings that contain them. `limit` is accepted as an alias for `per_page`. Not plan-gated."""
        params = {
            "ticker": ticker,
            "cik": cik,
            "from": from_,
            "to": to,
            "page": page,
            "per_page": per_page,
            "limit": limit,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/filings", params=params)
        return [FilingResponse._from_dict(item) for item in data]

    async def recent(self, *, ticker: str | None = None, per_page: int | None = None) -> list[FilingResponse]:
        """Get the most recently filed Form 4s, optionally filtered by ticker

        Returns the most recently filed Form 4s across all companies, newest first, optionally restricted to a single ticker. Use this to monitor new insider activity as it's ingested (e.g. a live "latest filings" feed) rather than for historical or bulk queries — for date-range or filter-heavy queries use GET /v1/transactions with from/to instead. Each entry includes the accession number, company ticker/name, period of report, filed date, amendment type, and the count of non-superseded transactions in that filing. There is no page parameter — this always returns the newest per_page filings, not an arbitrary offset. Not plan-gated."""
        params = {
            "ticker": ticker,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/filings/recent", params=params)
        return [FilingResponse._from_dict(item) for item in data]


class GeneratedForm144Resource:
    def __init__(self, client) -> None:
        self._client = client

    def list(self, *, ticker: str | None = None, insider_name: str | None = None, from_: str | None = None, to: str | None = None, exclude_10b5: bool | None = None, page: int | None = None, per_page: int | None = None) -> list[Form144Response]:
        """List Form 144 'notice of proposed sale' filings (Business plan+)

        Returns a paginated list of Form 144 notices — an insider's SEC filing declaring intent to sell restricted/control stock, filed BEFORE the actual sale (which later shows up as a Form 4 TransactionCode=S, typically ~2 days after). Use this as a leading indicator of upcoming insider selling; the isUnder10b5Plan flag on each row separates pre-scheduled 10b5-1 disposals from discretionary intent. Each row includes accession number, ticker/company, insider name/relationship, broker, shares proposed, aggregate market value, approximate sale date, exchange, and filed/notice dates. For a bulk historical pull use GET /v1/form144/export instead. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Query runs live against the database — no caching."""
        params = {
            "ticker": ticker,
            "insider_name": insider_name,
            "from": from_,
            "to": to,
            "exclude_10b5": exclude_10b5,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/form144", params=params)
        return [Form144Response._from_dict(item) for item in data]


class GeneratedAsyncForm144Resource:
    def __init__(self, client) -> None:
        self._client = client

    async def list(self, *, ticker: str | None = None, insider_name: str | None = None, from_: str | None = None, to: str | None = None, exclude_10b5: bool | None = None, page: int | None = None, per_page: int | None = None) -> list[Form144Response]:
        """List Form 144 'notice of proposed sale' filings (Business plan+)

        Returns a paginated list of Form 144 notices — an insider's SEC filing declaring intent to sell restricted/control stock, filed BEFORE the actual sale (which later shows up as a Form 4 TransactionCode=S, typically ~2 days after). Use this as a leading indicator of upcoming insider selling; the isUnder10b5Plan flag on each row separates pre-scheduled 10b5-1 disposals from discretionary intent. Each row includes accession number, ticker/company, insider name/relationship, broker, shares proposed, aggregate market value, approximate sale date, exchange, and filed/notice dates. For a bulk historical pull use GET /v1/form144/export instead. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Query runs live against the database — no caching."""
        params = {
            "ticker": ticker,
            "insider_name": insider_name,
            "from": from_,
            "to": to,
            "exclude_10b5": exclude_10b5,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/form144", params=params)
        return [Form144Response._from_dict(item) for item in data]


class GeneratedHoldingsResource:
    def __init__(self, client) -> None:
        self._client = client

    def list(self, *, ticker: str | None = None, cusip: str | None = None, manager_cik: str | None = None, quarter: str | None = None, min_value: float | None = None, page: int | None = None, per_page: int | None = None) -> list[HoldingResponse]:
        """List institutional holdings from Form 13F-HR (Business plan+)

        Returns a paginated list of individual position rows from Form 13F-HR institutional holdings reports (quarterly disclosures by managers with $100M+ AUM), most recent report period and highest value first. Each row includes the manager name/CIK, report period, issuer name/ticker, CUSIP, security class, position value and share count, share/voting authority type, and the source filing's accession number and filed date. A single security can appear multiple times per manager when sub-managers each report it separately (e.g. Berkshire's subsidiaries). Use GET /v1/managers instead when you want one row per manager (their latest filing + total AUM) rather than position-level detail. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Query runs live against the database — no caching; 13F data itself is inherently quarter-lagged (SEC filing deadline is 45 days after quarter end)."""
        params = {
            "ticker": ticker,
            "cusip": cusip,
            "manager_cik": manager_cik,
            "quarter": quarter,
            "min_value": min_value,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/holdings", params=params)
        return [HoldingResponse._from_dict(item) for item in data]

    def managers(self, *, name: str | None = None, min_aum: float | None = None, page: int | None = None, per_page: int | None = None) -> list[ManagerResponse]:
        """List institutional managers with their latest 13F-HR (Business plan+)

        Returns one row per institutional manager (13F filer), summarising their MOST RECENT 13F-HR filing — manager name/CIK, report period, total reported position value (AUM) and entry count, filed date, and accession number — ranked by AUM descending. Use this for manager-level discovery ("who are the biggest 13F filers?", "rank Apple's institutional holders") before drilling into position detail via GET /v1/holdings?manager_cik=. Amended (IsAmendment=true) filings are excluded from the 'latest' pick. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Query runs live against the database — no caching; 13F data is inherently quarter-lagged (SEC deadline is 45 days after quarter end)."""
        params = {
            "name": name,
            "min_aum": min_aum,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/managers", params=params)
        return [ManagerResponse._from_dict(item) for item in data]


class GeneratedAsyncHoldingsResource:
    def __init__(self, client) -> None:
        self._client = client

    async def list(self, *, ticker: str | None = None, cusip: str | None = None, manager_cik: str | None = None, quarter: str | None = None, min_value: float | None = None, page: int | None = None, per_page: int | None = None) -> list[HoldingResponse]:
        """List institutional holdings from Form 13F-HR (Business plan+)

        Returns a paginated list of individual position rows from Form 13F-HR institutional holdings reports (quarterly disclosures by managers with $100M+ AUM), most recent report period and highest value first. Each row includes the manager name/CIK, report period, issuer name/ticker, CUSIP, security class, position value and share count, share/voting authority type, and the source filing's accession number and filed date. A single security can appear multiple times per manager when sub-managers each report it separately (e.g. Berkshire's subsidiaries). Use GET /v1/managers instead when you want one row per manager (their latest filing + total AUM) rather than position-level detail. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Query runs live against the database — no caching; 13F data itself is inherently quarter-lagged (SEC filing deadline is 45 days after quarter end)."""
        params = {
            "ticker": ticker,
            "cusip": cusip,
            "manager_cik": manager_cik,
            "quarter": quarter,
            "min_value": min_value,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/holdings", params=params)
        return [HoldingResponse._from_dict(item) for item in data]

    async def managers(self, *, name: str | None = None, min_aum: float | None = None, page: int | None = None, per_page: int | None = None) -> list[ManagerResponse]:
        """List institutional managers with their latest 13F-HR (Business plan+)

        Returns one row per institutional manager (13F filer), summarising their MOST RECENT 13F-HR filing — manager name/CIK, report period, total reported position value (AUM) and entry count, filed date, and accession number — ranked by AUM descending. Use this for manager-level discovery ("who are the biggest 13F filers?", "rank Apple's institutional holders") before drilling into position detail via GET /v1/holdings?manager_cik=. Amended (IsAmendment=true) filings are excluded from the 'latest' pick. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Query runs live against the database — no caching; 13F data is inherently quarter-lagged (SEC deadline is 45 days after quarter end)."""
        params = {
            "name": name,
            "min_aum": min_aum,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/managers", params=params)
        return [ManagerResponse._from_dict(item) for item in data]


class GeneratedInsidersResource:
    def __init__(self, client) -> None:
        self._client = client

    def directory(self, *, letter: str | None = None, page: int | None = None, per_page: int | None = None) -> InsiderDirectoryResponse:
        """Browse insiders alphabetically by surname

        Returns the A-Z rail with a count per letter, plus one page of insiders under the
requested letter. Omit `letter` to get the rail and totals with no rows.

Names come from EDGAR surname-first ("HENNEMAN JOHN B III"), so alphabetical order
is order by surname. Casing in the source is inconsistent and is not normalised here.

This lists only insiders with at least 3 non-superseded transactions, capped at the
5,000 most active — the same set as the insiders sitemap shard, so the two cannot
drift. To find someone outside that set, use GET /v1/insiders?name= which searches
every filer. Rebuilt daily; `refreshedAt` reports when. Not plan-gated.

One row per FILER GROUP. A fund group files a single Form 4 listing several
reporting owners — the fund, its GP, its management company — and each is a real
EDGAR filer with its own CIK. Listing all of them spent about 11% of this capped
surface describing the same actors more than once, so browse shows one per group
and `filerGroupSize` says how many others share those exact transactions. The
others are not hidden: each keeps its own profile and is still returned by
GET /v1/insiders?name=."""
        params = {
            "letter": letter,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/insiders/directory", params=params)
        return InsiderDirectoryResponse._from_dict(data)

    def leaderboard(self, *, horizon: str | None = None, order: str | None = None, min_trades: int | None = None, limit: int | None = None) -> InsiderLeaderboardResponse:
        """Ranked leaderboard of insiders by buy track-record (Business plan+)

        Returns the top insiders ranked by historical buy performance — same scored-buy methodology as
GET /v1/insiders/{cik}/scorecard, applied across the whole corpus rather than one insider. Use
this to discover which insiders have the best track record; use the per-insider scorecard once
you have a specific CIK. Scores use absolute return (NOT market-adjusted) — a hit is a scored
buy with a positive 3m (or 6m) return anchored at the filing-date close. Only discretionary
open-market buys (P-code, not 10b5-1, not derivative) with a matured return are counted.
Insiders with fewer than min_trades (floor 5) scored buys are excluded. Requires Business plan
or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Results are cached for 1 hour per unique
parameter combination."""
        params = {
            "horizon": horizon,
            "order": order,
            "min_trades": min_trades,
            "limit": limit,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/insiders/leaderboard", params=params)
        return InsiderLeaderboardResponse._from_dict(data)

    def list(self, *, name: str | None = None, page: int | None = None, per_page: int | None = None) -> list[InsiderResponse]:
        """Search insiders (officers, directors, 10% owners) by name

        Searches insiders by name and returns a paginated list of matches with each insider's CIK, title, director/officer/10%-owner flags, and total filing count. Use this to resolve a person's name to their CIK before fetching their transaction history, career summary, or scorecard — the CIK returned here feeds directly into GET /v1/insiders/{cik}/transactions, /summary, and /scorecard. Omitting the name filter returns insiders in alphabetical order rather than performing a search. Not plan-gated — available on the Free tier."""
        params = {
            "name": name,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/insiders", params=params)
        return [InsiderResponse._from_dict(item) for item in data]

    def scorecard(self, cik: str) -> InsiderScorecardResponse:
        """Get insider buy track-record scorecard (Pro plan+)

        Returns the historical hit rate and average/median return of an insider's discretionary
open-market buys (TransactionCode=P, excluding 10b5-1 plans and derivatives), plus their best
and worst scored buy. Scores use absolute return (NOT market-adjusted) anchored at the
filing-date close. A 'hit' is a scored buy whose 3m (or 6m) return is positive. Use this over
GET /v1/insiders/{cik}/summary when you specifically want a scored track record (with a
sample-sufficiency guard) rather than raw totals; use GET /v1/insiders/leaderboard (Business+)
to rank many insiders by this same methodology. Score fields (hitRate3m, avgReturn3m, etc.) are
null when the insider has fewer than 5 matured scored buys (sampleSufficient=false), preventing
misleading statistics from small samples. Requires Pro plan or higher (402 PLAN_REQUIRED on
Free/Starter). Returns 404 NOT_FOUND if the CIK isn't tracked. Computed live — no caching.
Note: all return fields (HitRate3m, AvgReturn3m, MedianReturn3m, etc.) are stored
as FRACTIONS — 0.05 means +5%, -0.10 means -10%."""
        data = self._client._get(f"/v1/insiders/{cik}/scorecard")
        return InsiderScorecardResponse._from_dict(data)

    def summary(self, cik: str) -> InsiderSummaryResponse:
        """Get aggregate career summary for an insider (Pro plan+)

        Aggregate career roll-up for a single insider: first/last transaction dates, total transactions,
total bought/sold/net USD, transaction-code breakdown (P/S/F/M/A/G/other counts), the top 10
companies traded (by activity), the 10b5-1 vs discretionary split, and average post-trade returns
across discretionary open-market (non-10b5-1) buy/sell trades. Use this for a one-call career
overview rather than paging through GET /v1/insiders/{cik}/transactions yourself; for a scored
hit-rate track record on buys specifically, use GET /v1/insiders/{cik}/scorecard instead.
Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Returns 404 NOT_FOUND if the CIK
isn't tracked. Computed live from current data — no caching. Note: all return fields (AvgReturn1d,
AvgReturn1w, etc.) are stored as FRACTIONS — 0.05 means +5%."""
        data = self._client._get(f"/v1/insiders/{cik}/summary")
        return InsiderSummaryResponse._from_dict(data)


class GeneratedAsyncInsidersResource:
    def __init__(self, client) -> None:
        self._client = client

    async def directory(self, *, letter: str | None = None, page: int | None = None, per_page: int | None = None) -> InsiderDirectoryResponse:
        """Browse insiders alphabetically by surname

        Returns the A-Z rail with a count per letter, plus one page of insiders under the
requested letter. Omit `letter` to get the rail and totals with no rows.

Names come from EDGAR surname-first ("HENNEMAN JOHN B III"), so alphabetical order
is order by surname. Casing in the source is inconsistent and is not normalised here.

This lists only insiders with at least 3 non-superseded transactions, capped at the
5,000 most active — the same set as the insiders sitemap shard, so the two cannot
drift. To find someone outside that set, use GET /v1/insiders?name= which searches
every filer. Rebuilt daily; `refreshedAt` reports when. Not plan-gated.

One row per FILER GROUP. A fund group files a single Form 4 listing several
reporting owners — the fund, its GP, its management company — and each is a real
EDGAR filer with its own CIK. Listing all of them spent about 11% of this capped
surface describing the same actors more than once, so browse shows one per group
and `filerGroupSize` says how many others share those exact transactions. The
others are not hidden: each keeps its own profile and is still returned by
GET /v1/insiders?name=."""
        params = {
            "letter": letter,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/insiders/directory", params=params)
        return InsiderDirectoryResponse._from_dict(data)

    async def leaderboard(self, *, horizon: str | None = None, order: str | None = None, min_trades: int | None = None, limit: int | None = None) -> InsiderLeaderboardResponse:
        """Ranked leaderboard of insiders by buy track-record (Business plan+)

        Returns the top insiders ranked by historical buy performance — same scored-buy methodology as
GET /v1/insiders/{cik}/scorecard, applied across the whole corpus rather than one insider. Use
this to discover which insiders have the best track record; use the per-insider scorecard once
you have a specific CIK. Scores use absolute return (NOT market-adjusted) — a hit is a scored
buy with a positive 3m (or 6m) return anchored at the filing-date close. Only discretionary
open-market buys (P-code, not 10b5-1, not derivative) with a matured return are counted.
Insiders with fewer than min_trades (floor 5) scored buys are excluded. Requires Business plan
or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Results are cached for 1 hour per unique
parameter combination."""
        params = {
            "horizon": horizon,
            "order": order,
            "min_trades": min_trades,
            "limit": limit,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/insiders/leaderboard", params=params)
        return InsiderLeaderboardResponse._from_dict(data)

    async def list(self, *, name: str | None = None, page: int | None = None, per_page: int | None = None) -> list[InsiderResponse]:
        """Search insiders (officers, directors, 10% owners) by name

        Searches insiders by name and returns a paginated list of matches with each insider's CIK, title, director/officer/10%-owner flags, and total filing count. Use this to resolve a person's name to their CIK before fetching their transaction history, career summary, or scorecard — the CIK returned here feeds directly into GET /v1/insiders/{cik}/transactions, /summary, and /scorecard. Omitting the name filter returns insiders in alphabetical order rather than performing a search. Not plan-gated — available on the Free tier."""
        params = {
            "name": name,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/insiders", params=params)
        return [InsiderResponse._from_dict(item) for item in data]

    async def scorecard(self, cik: str) -> InsiderScorecardResponse:
        """Get insider buy track-record scorecard (Pro plan+)

        Returns the historical hit rate and average/median return of an insider's discretionary
open-market buys (TransactionCode=P, excluding 10b5-1 plans and derivatives), plus their best
and worst scored buy. Scores use absolute return (NOT market-adjusted) anchored at the
filing-date close. A 'hit' is a scored buy whose 3m (or 6m) return is positive. Use this over
GET /v1/insiders/{cik}/summary when you specifically want a scored track record (with a
sample-sufficiency guard) rather than raw totals; use GET /v1/insiders/leaderboard (Business+)
to rank many insiders by this same methodology. Score fields (hitRate3m, avgReturn3m, etc.) are
null when the insider has fewer than 5 matured scored buys (sampleSufficient=false), preventing
misleading statistics from small samples. Requires Pro plan or higher (402 PLAN_REQUIRED on
Free/Starter). Returns 404 NOT_FOUND if the CIK isn't tracked. Computed live — no caching.
Note: all return fields (HitRate3m, AvgReturn3m, MedianReturn3m, etc.) are stored
as FRACTIONS — 0.05 means +5%, -0.10 means -10%."""
        data = await self._client._get(f"/v1/insiders/{cik}/scorecard")
        return InsiderScorecardResponse._from_dict(data)

    async def summary(self, cik: str) -> InsiderSummaryResponse:
        """Get aggregate career summary for an insider (Pro plan+)

        Aggregate career roll-up for a single insider: first/last transaction dates, total transactions,
total bought/sold/net USD, transaction-code breakdown (P/S/F/M/A/G/other counts), the top 10
companies traded (by activity), the 10b5-1 vs discretionary split, and average post-trade returns
across discretionary open-market (non-10b5-1) buy/sell trades. Use this for a one-call career
overview rather than paging through GET /v1/insiders/{cik}/transactions yourself; for a scored
hit-rate track record on buys specifically, use GET /v1/insiders/{cik}/scorecard instead.
Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Returns 404 NOT_FOUND if the CIK
isn't tracked. Computed live from current data — no caching. Note: all return fields (AvgReturn1d,
AvgReturn1w, etc.) are stored as FRACTIONS — 0.05 means +5%."""
        data = await self._client._get(f"/v1/insiders/{cik}/summary")
        return InsiderSummaryResponse._from_dict(data)


class GeneratedSignalsResource:
    def __init__(self, client) -> None:
        self._client = client

    def convergence(self, *, ticker: str | None = None, window_days: int | None = None, lookback_days: int | None = None, page: int | None = None, per_page: int | None = None) -> list[ConvergenceEntryDto]:
        """Insider cluster-buy x congressional-purchase convergence (Pro plan+)

        Returns the tickers where an insider cluster-buy (InsiderSignal.IsClusterBuy) and at least one non-superseded congressional PURCHASE happened within window_days of EACH OTHER, restricted to convergences where the MORE RECENT of the pair's two dates is within a trailing lookback_days (so this surfaces CURRENT convergences, not ancient history). DEFINITION: for each result, insider.signalDate is the SignalDate of the qualifying cluster-buy signal with the most recent date (insider.insiderCount is that same signal's count — never summed or maxed across multiple signals), and congress is every non-superseded congressional purchase that paired with at least one qualifying cluster-buy (not every purchase in the window — only the ones that actually paired). firstSeen/lastSeen are the earliest/most recent dates among all qualifying insider and congress dates for that ticker. STRENGTH is documented arithmetic, NOT a black-box or predictive/ML score: strength = (distinct congressional purchasers among the qualifying legs) x (the representative signal's insiderCount) — a plain multiplication of two observed counts, nothing more. HONESTY: every congress leg always carries both amountLow and amountHigh (STOCK Act discloses ranges, never exact figures — never combined into a fabricated midpoint) and disclosureLagDays = (disclosureDate - transactionDate); congressional trades are disclosed up to 45 days after the actual trade under the STOCK Act, so this endpoint is detection/monitoring of what insiders AND members of Congress have DISCLOSED buying, not a claim of predictive edge, alpha, or win rate — no performance numbers are computed or implied anywhere in this response. window_days and lookback_days are both caller-overridable with clamps (see each parameter's own description for the exact bounds). Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Query runs live against the database — no caching."""
        params = {
            "ticker": ticker,
            "window_days": window_days,
            "lookback_days": lookback_days,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/signals/convergence", params=params)
        return [ConvergenceEntryDto._from_dict(item) for item in data]

    def explain(self, ticker: str, *, date: str | None = None) -> SignalExplanation:
        """Explain why a signal fired: the insiders and trades counted, what was excluded, and the criteria (Business plan+)

        Reconstructs the full evidence behind one company's insider signal from GET /v1/signals: the detection criteria (5-day cluster window, 3-insider threshold, 90-day ratio window, 10b5-1 exclusion), the list of cluster buyers and sellers (each with their role and individual trades in the window), trades that were excluded from the cluster count and why (10b5-1 plan or superseded by amendment), and the raw buy/sell share totals behind the 90-day ratio. Use this to audit or debug a specific signal rather than to scan many companies (use GET /v1/signals for that). This is a LIVE reconstruction from current non-superseded data, computed on every request (no caching) — it can differ slightly from the originally stored signal if trades were amended afterward. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Returns 404 COMPANY_NOT_FOUND if the ticker isn't tracked, 404 SIGNAL_NOT_FOUND if no signal exists for the given/most-recent date, or 400 INVALID_DATE if `date` isn't YYYY-MM-DD."""
        params = {
            "date": date,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/signals/{ticker}/explain", params=params)
        return SignalExplanation._from_dict(data)

    def sentiment(self, ticker: str, *, months: int | None = None) -> SentimentResponse:
        """Monthly insider sentiment score for a ticker (Business plan+)

        Returns a monthly time series of an MSPR-style insider sentiment score for one company: for each month, (buyValue - sellValue) / (buyValue + sellValue) * 100, alongside the raw buy/sell USD totals and trade counts. Only open-market P/S trades are counted, and — unlike Finnhub's MSPR — 10b5-1 plan trades and derivative transactions are excluded, so the score reflects discretionary conviction rather than pre-scheduled or compensation-driven activity. Use this for a trend view of buying/selling pressure over time; for a single point-in-time cluster/ratio snapshot use GET /v1/signals instead. Score is on a -100 (all selling) to +100 (all buying) scale — NOT a fraction, unlike the return fields on other endpoints. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Returns 404 NOT_FOUND if the ticker isn't tracked. Cached for 60 seconds per (ticker, months) combination."""
        params = {
            "months": months,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = self._client._get(f"/v1/signals/sentiment/{ticker}", params=params)
        return SentimentResponse._from_dict(data)


class GeneratedAsyncSignalsResource:
    def __init__(self, client) -> None:
        self._client = client

    async def convergence(self, *, ticker: str | None = None, window_days: int | None = None, lookback_days: int | None = None, page: int | None = None, per_page: int | None = None) -> list[ConvergenceEntryDto]:
        """Insider cluster-buy x congressional-purchase convergence (Pro plan+)

        Returns the tickers where an insider cluster-buy (InsiderSignal.IsClusterBuy) and at least one non-superseded congressional PURCHASE happened within window_days of EACH OTHER, restricted to convergences where the MORE RECENT of the pair's two dates is within a trailing lookback_days (so this surfaces CURRENT convergences, not ancient history). DEFINITION: for each result, insider.signalDate is the SignalDate of the qualifying cluster-buy signal with the most recent date (insider.insiderCount is that same signal's count — never summed or maxed across multiple signals), and congress is every non-superseded congressional purchase that paired with at least one qualifying cluster-buy (not every purchase in the window — only the ones that actually paired). firstSeen/lastSeen are the earliest/most recent dates among all qualifying insider and congress dates for that ticker. STRENGTH is documented arithmetic, NOT a black-box or predictive/ML score: strength = (distinct congressional purchasers among the qualifying legs) x (the representative signal's insiderCount) — a plain multiplication of two observed counts, nothing more. HONESTY: every congress leg always carries both amountLow and amountHigh (STOCK Act discloses ranges, never exact figures — never combined into a fabricated midpoint) and disclosureLagDays = (disclosureDate - transactionDate); congressional trades are disclosed up to 45 days after the actual trade under the STOCK Act, so this endpoint is detection/monitoring of what insiders AND members of Congress have DISCLOSED buying, not a claim of predictive edge, alpha, or win rate — no performance numbers are computed or implied anywhere in this response. window_days and lookback_days are both caller-overridable with clamps (see each parameter's own description for the exact bounds). Requires Pro plan or higher (402 PLAN_REQUIRED on Free/Starter). Query runs live against the database — no caching."""
        params = {
            "ticker": ticker,
            "window_days": window_days,
            "lookback_days": lookback_days,
            "page": page,
            "per_page": per_page,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/signals/convergence", params=params)
        return [ConvergenceEntryDto._from_dict(item) for item in data]

    async def explain(self, ticker: str, *, date: str | None = None) -> SignalExplanation:
        """Explain why a signal fired: the insiders and trades counted, what was excluded, and the criteria (Business plan+)

        Reconstructs the full evidence behind one company's insider signal from GET /v1/signals: the detection criteria (5-day cluster window, 3-insider threshold, 90-day ratio window, 10b5-1 exclusion), the list of cluster buyers and sellers (each with their role and individual trades in the window), trades that were excluded from the cluster count and why (10b5-1 plan or superseded by amendment), and the raw buy/sell share totals behind the 90-day ratio. Use this to audit or debug a specific signal rather than to scan many companies (use GET /v1/signals for that). This is a LIVE reconstruction from current non-superseded data, computed on every request (no caching) — it can differ slightly from the originally stored signal if trades were amended afterward. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Returns 404 COMPANY_NOT_FOUND if the ticker isn't tracked, 404 SIGNAL_NOT_FOUND if no signal exists for the given/most-recent date, or 400 INVALID_DATE if `date` isn't YYYY-MM-DD."""
        params = {
            "date": date,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/signals/{ticker}/explain", params=params)
        return SignalExplanation._from_dict(data)

    async def sentiment(self, ticker: str, *, months: int | None = None) -> SentimentResponse:
        """Monthly insider sentiment score for a ticker (Business plan+)

        Returns a monthly time series of an MSPR-style insider sentiment score for one company: for each month, (buyValue - sellValue) / (buyValue + sellValue) * 100, alongside the raw buy/sell USD totals and trade counts. Only open-market P/S trades are counted, and — unlike Finnhub's MSPR — 10b5-1 plan trades and derivative transactions are excluded, so the score reflects discretionary conviction rather than pre-scheduled or compensation-driven activity. Use this for a trend view of buying/selling pressure over time; for a single point-in-time cluster/ratio snapshot use GET /v1/signals instead. Score is on a -100 (all selling) to +100 (all buying) scale — NOT a fraction, unlike the return fields on other endpoints. Requires Business plan or higher (402 PLAN_REQUIRED on Free/Starter/Pro). Returns 404 NOT_FOUND if the ticker isn't tracked. Cached for 60 seconds per (ticker, months) combination."""
        params = {
            "months": months,
        }
        params = {k: str(v) for k, v in params.items() if v is not None}
        data = await self._client._get(f"/v1/signals/sentiment/{ticker}", params=params)
        return SentimentResponse._from_dict(data)


class GeneratedStatsResource:
    def __init__(self, client) -> None:
        self._client = client

    def get(self) -> CorpusStats:
        """Public corpus-wide statistics — no API key required

        Returns headline dataset totals: filing count, transaction count, tracked companies, institutional holdings rows, Form 144 and Form 13F-HR filing counts, the earliest filing date in the corpus, the most recent quarter's total 13F-HR reported AUM in USD, and measured ingestion latency (median/p95 seconds from SEC acceptance to our processing, trailing 7 days). Use this for corpus-wide totals (e.g. a marketing/status widget), not for per-company or per-insider data — those live under GET /v1/companies and GET /v1/insiders. For freshness and coverage-quality metrics (is ingestion stalled, is price data stale) use GET /v1/data-quality instead. Takes no parameters. No API key or plan required. Cached for ~12 hours."""
        data = self._client._get(f"/v1/stats")
        return CorpusStats._from_dict(data)


class GeneratedAsyncStatsResource:
    def __init__(self, client) -> None:
        self._client = client

    async def get(self) -> CorpusStats:
        """Public corpus-wide statistics — no API key required

        Returns headline dataset totals: filing count, transaction count, tracked companies, institutional holdings rows, Form 144 and Form 13F-HR filing counts, the earliest filing date in the corpus, the most recent quarter's total 13F-HR reported AUM in USD, and measured ingestion latency (median/p95 seconds from SEC acceptance to our processing, trailing 7 days). Use this for corpus-wide totals (e.g. a marketing/status widget), not for per-company or per-insider data — those live under GET /v1/companies and GET /v1/insiders. For freshness and coverage-quality metrics (is ingestion stalled, is price data stale) use GET /v1/data-quality instead. Takes no parameters. No API key or plan required. Cached for ~12 hours."""
        data = await self._client._get(f"/v1/stats")
        return CorpusStats._from_dict(data)


class GeneratedStatusResource:
    def __init__(self, client) -> None:
        self._client = client

    def history(self) -> UptimeHistoryResponse:
        """Measured uptime history for the public status page — trailing 90-day daily breakdown

        Returns a daily breakdown of measured API uptime over a trailing 90-day window, computed from an internal heartbeat probe that runs every 5 minutes and performs the same DB-connectivity check as GET /health/ready. Each day in the `days` array reports the number of 5-minute slots expected to have elapsed (288 for a complete past day, pro-rated for the feature's first day and for today's partial day), how many of those slots recorded a healthy heartbeat, and the resulting uptime percentage for that day — plus an overall percentage (`overallPct`) across the whole window. `start` is the earliest date included: either the date of the very first heartbeat ever recorded, or 89 days before today once more than 90 days of history exist. Days before that are never returned. Use this to render an uptime history / status bar; for live corpus freshness use GET /v1/data-quality instead. Takes no parameters. Cached for ~5 minutes; no API key or plan required."""
        data = self._client._get(f"/v1/status/history")
        return UptimeHistoryResponse._from_dict(data)


class GeneratedAsyncStatusResource:
    def __init__(self, client) -> None:
        self._client = client

    async def history(self) -> UptimeHistoryResponse:
        """Measured uptime history for the public status page — trailing 90-day daily breakdown

        Returns a daily breakdown of measured API uptime over a trailing 90-day window, computed from an internal heartbeat probe that runs every 5 minutes and performs the same DB-connectivity check as GET /health/ready. Each day in the `days` array reports the number of 5-minute slots expected to have elapsed (288 for a complete past day, pro-rated for the feature's first day and for today's partial day), how many of those slots recorded a healthy heartbeat, and the resulting uptime percentage for that day — plus an overall percentage (`overallPct`) across the whole window. `start` is the earliest date included: either the date of the very first heartbeat ever recorded, or 89 days before today once more than 90 days of history exist. Days before that are never returned. Use this to render an uptime history / status bar; for live corpus freshness use GET /v1/data-quality instead. Takes no parameters. Cached for ~5 minutes; no API key or plan required."""
        data = await self._client._get(f"/v1/status/history")
        return UptimeHistoryResponse._from_dict(data)

